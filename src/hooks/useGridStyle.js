import { styled } from '@mui/material/styles';

// Replace this with a proper implementation based on your actual file
// This is just a placeholder example
const useGridStyles = () => {
  return {
    paper: {
      padding: 2,
      textAlign: 'center',
    }
  };
};

export default useGridStyles;
